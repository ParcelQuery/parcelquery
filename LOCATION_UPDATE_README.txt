LOCATION-ONLY UPDATE

Replace only script.js and admin.js in your existing ParcelQuery repository.
Do NOT replace index.html, style.css, admin.html, images, or other existing files.

Run the location columns SQL in Supabase if you have not already done so.
